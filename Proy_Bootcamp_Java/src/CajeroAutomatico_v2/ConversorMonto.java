package CajeroAutomatico_v2;

public class ConversorMonto {
    public static String convertirNumeroALetras(double monto) {
        // Implementación simplificada. Agregar lógica completa si se requiere.
        return monto == 1000 ? "mil" : String.valueOf(monto);
    }
}
