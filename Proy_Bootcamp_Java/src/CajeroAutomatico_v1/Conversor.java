package CajeroAutomatico_v1;

public class Conversor {
    public static String convertirNumeroALetras(double monto) {
        // Aquí iría el código para convertir números a letras (simplificado)
        if (monto == 1000) return "mil con ceros";
        return String.valueOf(monto); // Sustituir con la lógica completa
    }
}
